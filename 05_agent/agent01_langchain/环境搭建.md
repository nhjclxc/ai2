
```shell

## 1️⃣ 初始化项目
uv init agent01_langchain
cd agent01_langchain
## 2️⃣ 创建虚拟环境
uv venv --python 3.11
## 3️⃣ 激活环境
# Windows 
.venv\Scripts\activate
# Linux/macOS
source .venv/bin/activate
## 4️⃣ 安装依赖
uv add openai
uv add langchain==1.2.0
# LangChain 1.x 架构是模块化的，上面一个是LangChain的核心包，下面是一些第三方的大模型 sdk 要调用这些大模型也必须安装
# OpenAI 支持
uv add langchain-openai
# 社区工具包
uv add langchain-community
# 阿里通义千问
uv add dashscope
# deepseek，deepseek可以直接是由 openai 的包
uv add langchain-deepseek
# 豆包，豆包的不是langchain不是langchain系的包是单独的
uv add 'volcengine-python-sdk[ark]'
# 第三方统一 SDK（更爽）以下两个是别人帮你封装好的第三方包，很多公司的sdk都在里面了（可选）
uv add litellm
uv add my-llm-sdk
# 向量数据库（可选）
uv add chromadb
# 用于读取.env文件里面的key
uv add python-dotenv
## 5️⃣ 运行代码
uv run python main.py
## 6️⃣ 输出当前依赖
uv pip freeze > requirements.txt
## 锁定依赖
uv lock
## 安装依赖
pip install -r requirements.txt
uv pip install -r requirements.txt

```

```其他环境
uv add sqlalchemy pymysql
uv add redis

# 使用 tiktoken 来计算每一次消息的 token 数量
uv add tiktoken

# JSONLoader所需要的而外包, JSONLoader底层依赖jq
uv add jq
# PyPderLoader 所需要的而外包, PyPderLoader 底层依赖 pypdf
uv add pypdf

# 文件向量数据库
uv add langchain_chroma chromadb

uv add streamlit 
```


```

写个简单测试：main.py

from langchain_openai import ChatOpenAI

llm = ChatOpenAI(
    model="gpt-4o-mini",
    api_key="your-api-key"
)

print(llm.invoke("你好"))
```

